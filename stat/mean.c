#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main(int argc, char *argv[]) {
	if (argc >= 2UL) {
		size_t array_count = 0UL;
		for (size_t iterator = 1UL; iterator < argc; iterator += 1UL) {
			
		}
	}
	return 0;
}